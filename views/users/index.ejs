<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>A</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css">
    <style>
      body {
      margin: 0;
      padding: 0;
      font-family: Arial, sans-serif;
    }

    .jack {
      background-color: #b84ce6;
      color: #fff;
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 10px;
    }

    .jack-logo {
      font-size: 24px;
      font-weight: bold;
    }

    .jackl {
      list-style-type: none;
      margin: 0;
      padding: 0;
    }

    .jackl li {
      display: inline;
      margin-left: 20px;
    }

    .jackl li a {
      color: #fff;
      text-decoration: none;
    }

    .jackl li a:hover {
      text-decoration: underline;
    }

    .login-button {
      background-color: #fff;
      color: #333;
      padding: 8px 15px;
      border: none;
      border-radius: 5px;
      cursor: pointer;
    }
    .d-flex{
        justify-content: center;
        align-items: center;
    }

    footer {
  background-color: #b84ce6;
  color: #fff;
  padding: 20px 0;
  text-align: center;
}

.workfoot {
  max-width: 960px;
  margin: 0 auto;
}

    </style>
</head>
<body>
    <div class="jack">
        <div class="jack-logo">E-Commerce</div>
        <ul class="jackl">
          <li><a href="/users">Home</a></li>
          <li><a href="/users">Products</a></li>
          <li><a href="/users/new">New</a></li>
        </ul>
        <button class="login-button">Login</button>
    </div>
    <section class="d-flex flex-wrap m-5" >
        <% for( let user of users ) { %>
            <div class="card mx-3 border border-5 h-100" style="width: 18rem;">
                <img src="<%= user.photo %>" class="card-img-top" alt="...">
                <div class="card-body">
                    <h5 class="card-title"><%= user.name %></h5>
                     <h3> <%= user.price %></h3>
                     <p><%= user.description%></p>      
                    <!-- <a href="/users/<%= user.id %>">Show</a>
                    <a href="/users/<%= user.id %>/edit">Edit</a> --> 
                         <a href="/users/<%= user.id %>/edit">Edit</a><br>
                    <form action="/users/<%= user._id %>" method="get">
                        <button style="background-color: rgb(148, 185, 248); color: black;">Buy now</button>
                    </form>
                </div>
            </div>
        <% } %>

    </section>
    <footer>
        <div class="workfoot">
          <p style="font-size: 14px;margin: 0;">&copy; @copyright All rights reserved.</p>
        </div>
    </footer>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>